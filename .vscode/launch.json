{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "program": "${workspaceFolder}\\dist\\Strategy.js"
        }
    ]
}
